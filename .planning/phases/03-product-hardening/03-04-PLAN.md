---
phase: 03-product-hardening
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/features/content/services/media-validation.ts
  - src/features/content/services/media-validation.test.ts
  - src/features/content/services/upload-service.ts
  - src/app/api/content-projects/route.ts
  - src/app/api/content-projects/[id]/media/route.ts
  - src/features/content/components/create-content-form.tsx
autonomous: true
requirements: [MED-04, SEC-03]
must_haves:
  truths:
    - "Oversized individual files are rejected before storage writes."
    - "Oversized total uploads are rejected before processing."
    - "Too many images or multiple audio files are rejected."
    - "Spoofed MIME types are rejected by file signature checks."
    - "Audio duration over the configured limit is rejected before video processing."
  artifacts:
    - path: "src/features/content/services/media-validation.ts"
      provides: "Upload limit, signature, count, and audio duration validation"
      exports: ["validateMediaFiles", "detectMediaSignature"]
    - path: "src/features/content/services/media-validation.test.ts"
      provides: "Unit coverage for upload validation edge cases"
    - path: "src/features/content/services/upload-service.ts"
      provides: "Validation integration before storage writes"
  key_links:
    - from: "src/features/content/services/upload-service.ts"
      to: "src/features/content/services/media-validation.ts"
      via: "validate before saveUploadedFile"
      pattern: "validateMediaFiles"
---

<objective>
Reject unsafe or excessive uploads before storage and expensive processing.

Purpose: MED-04 requires size/count/duration limits, and SEC-03 requires basic abuse resistance on upload entry points.
Output: Shared media validation service, API/action integration, UI helper copy, and focused tests.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-product-hardening/03-CONTEXT.md
@.planning/phases/03-product-hardening/03-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/03-product-hardening/03-02-SUMMARY.md
@.planning/phases/03-product-hardening/03-03-SUMMARY.md

<interfaces>
Existing upload helpers: `filesFromFormData(formData, field)`, `validateProjectFiles(files, options)`, `attachMediaFilesToProject(projectId, files)`, `createProjectWithUploads(input, files)`, and `parseProjectFormData(formData)`.

Existing UI copy from 03-UI-SPEC: upload validation error and upload limits helper.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create media validation service and tests</name>
  <files>src/features/content/services/media-validation.ts, src/features/content/services/media-validation.test.ts</files>
  <behavior>
    - PNG/JPEG/WebP signatures match accepted image types.
    - MP3/WAV/M4A signatures match accepted audio types.
    - MIME/signature mismatches fail with a safe category: `formato`.
    - Limit failures include category: `imagem`, `audio`, `tamanho`, `quantidade`, or `duracao`.
  </behavior>
  <action>Add a pure-ish validation module with constants for max image count, max one audio file, max per-file bytes, max total bytes, and max audio seconds. Use file header bytes for signature checks instead of browser MIME alone. For duration, implement a helper that uses ffprobe when available but is mockable in tests; validation must happen before saving files. Write Vitest coverage with synthetic File objects/buffers and mocked duration probe.</action>
  <verify>
    <automated>npm test -- --run src/features/content/services/media-validation.test.ts</automated>
  </verify>
  <done>Upload validation tests cover valid files, spoofed MIME, oversize, over-count, multiple audio, and long audio.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce validation in upload service and APIs</name>
  <files>src/features/content/services/upload-service.ts, src/app/api/content-projects/route.ts, src/app/api/content-projects/[id]/media/route.ts</files>
  <behavior>
    - Validation runs before `saveUploadedFile`.
    - Both create-project and attach-media API paths use the same validation rules.
    - Rejected uploads return safe Portuguese errors and do not leave project files behind.
  </behavior>
  <action>Replace MIME-only validation with `validateMediaFiles`. Ensure create and attach flows validate before storage writes and before Gemini/FFmpeg/Whisper work. Keep rollback cleanup for unexpected failures. Map validation categories to the UI-spec upload error copy without raw filenames if filenames contain unsafe text.</action>
  <verify>
    <automated>npm test -- --run src/features/content/services/media-validation.test.ts</automated>
  </verify>
  <done>All upload entry points share the same limits and signature checks.</done>
</task>

<task type="auto">
  <name>Task 3: Surface upload limit guidance in the create form</name>
  <files>src/features/content/components/create-content-form.tsx</files>
  <action>Add persistent helper copy from 03-UI-SPEC near upload controls: `Aceitamos PNG, JPG ou WebP para imagens e MP3, WAV ou M4A para audio, dentro dos limites do sistema.` Keep existing layout and do not introduce a new design system. Make rejected upload query/banner states use the safe upload validation copy.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Create form shows limit guidance and build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->multipart upload | Untrusted bytes and metadata enter the app. |
| upload validation->local storage | Accepted bytes are written to disk and later processed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | D | upload service | mitigate | Enforce max file size, total size, image count, audio count, and audio duration before writes. |
| T-03-04-02 | T | media validation | mitigate | Check file signatures instead of trusting `File.type`. |
| T-03-04-03 | I | upload errors | mitigate | Return category-based safe messages, not raw parser/ffprobe diagnostics. |
</threat_model>

<verification>
Run `npm test -- --run src/features/content/services/media-validation.test.ts`, `npm run lint`, and `npm run build`.
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| REQ | MED-04 size/count/duration limits before processing | This plan |
| REQ | SEC-03 basic abuse protection on uploads | This plan |
| CONTEXT | Validate file signatures, keep validation isolated for future S3/R2 | This plan |
| UI-SPEC | Upload helper and rejected upload banner copy | This plan |
| Deferred | Object storage migration | Excluded; validation module is storage-agnostic |
</source_audit>

<success_criteria>
Invalid, excessive, spoofed, or too-long media is rejected before storage writes or media processing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-hardening/03-04-SUMMARY.md`.
</output>
