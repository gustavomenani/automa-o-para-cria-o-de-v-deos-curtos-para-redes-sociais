---
phase: 03-product-hardening
plan: 07
type: execute
wave: 5
depends_on: [03-04, 03-05, 03-06]
files_modified:
  - src/lib/storage/local-storage.test.ts
  - src/features/schedule/schedule-validation.test.ts
  - src/features/content/services/upload-service.test.ts
  - src/features/content/actions.test.ts
  - src/features/video/services/caption-helpers.test.ts
autonomous: true
requirements: [OPS-05, MED-04, VID-06, CAP-06, SCH-04, SEC-02, SEC-03]
must_haves:
  truths:
    - "High-risk pure/domain logic has automated tests."
    - "Storage path guards reject traversal and unsafe delete paths."
    - "Upload validation and deletion cleanup are covered."
    - "Schedule validation and concurrency guard behavior are covered."
    - "Caption helper coverage includes low-confidence review triggers."
  artifacts:
    - path: "src/lib/storage/local-storage.test.ts"
      provides: "Storage path guard and cleanup tests"
    - path: "src/features/schedule/schedule-validation.test.ts"
      provides: "Future/past schedule validation tests"
    - path: "src/features/content/services/upload-service.test.ts"
      provides: "Upload service integration validation tests"
    - path: "src/features/content/actions.test.ts"
      provides: "Deletion cleanup and ownership action tests"
  key_links:
    - from: "src/features/content/actions.test.ts"
      to: "src/features/content/actions.ts"
      via: "mocked Prisma/storage delete assertions"
      pattern: "deleteContentProjectAction"
    - from: "src/features/schedule/schedule-validation.test.ts"
      to: "src/features/schedule/actions.ts"
      via: "shared validator or exported helper"
      pattern: "scheduledAt"
---

<objective>
Add the Phase 3 automated test net for the highest-risk hardening logic.

Purpose: OPS-05 requires automated tests for risky flows, and Phase 3 changed security, uploads, scheduling, generation locks, and caption review.
Output: Focused Vitest coverage that can run locally with `npm test` without real FFmpeg, Whisper, or provider credentials.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-product-hardening/03-CONTEXT.md
@.planning/codebase/TESTING.md
@.planning/phases/01-stabilize-core-mvp/01-01-SUMMARY.md
@.planning/phases/03-product-hardening/03-04-SUMMARY.md
@.planning/phases/03-product-hardening/03-05-SUMMARY.md
@.planning/phases/03-product-hardening/03-06-SUMMARY.md

<interfaces>
Vitest is available through `npm test`. Do not require a real database, real provider credentials, FFmpeg, ffprobe, Python, or Whisper for these tests. Mock Prisma and process/media probes where necessary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cover storage guards and deletion cleanup</name>
  <files>src/lib/storage/local-storage.test.ts, src/features/content/actions.test.ts</files>
  <behavior>
    - Storage delete refuses paths outside `storageRoot`, including Windows separator traversal cases.
    - Delete action captures media/generated paths for an owned project and invokes cleanup.
    - Cleanup failures do not expose raw filesystem paths to user feedback.
  </behavior>
  <action>Add Vitest tests with temporary directories and mocked Prisma/storage where needed. Keep assertions focused on behavior, not implementation details. Do not touch real `storage/uploads` or `storage/generated` contents.</action>
  <verify>
    <automated>npm test -- --run src/lib/storage/local-storage.test.ts src/features/content/actions.test.ts</automated>
  </verify>
  <done>Storage guard and delete cleanup tests pass without using production storage paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cover upload and schedule validators</name>
  <files>src/features/content/services/upload-service.test.ts, src/features/schedule/schedule-validation.test.ts</files>
  <behavior>
    - Upload service rejects invalid signatures, over-limit counts/sizes, multiple audio, and long audio through the integrated service path.
    - Schedule validator rejects past/current timestamps and accepts future timestamps across local timezone inputs.
  </behavior>
  <action>Add focused tests around the public validation/service functions created in Plans 03-04 and 03-06. If schedule validation is still inline inside the action, extract a small pure helper so the test can assert `input -> expected` without mocking the full action.</action>
  <verify>
    <automated>npm test -- --run src/features/content/services/upload-service.test.ts src/features/schedule/schedule-validation.test.ts</automated>
  </verify>
  <done>Upload and schedule validation behavior is covered by deterministic tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend caption helper and generation guard coverage</name>
  <files>src/features/video/services/caption-helpers.test.ts, src/features/video/services/video-service.test.ts</files>
  <behavior>
    - Caption quality tests include the low-confidence/fallback condition that triggers review UI.
    - Generation guard tests from Plan 03-05 remain passing alongside the full suite.
  </behavior>
  <action>Extend existing caption helper tests rather than duplicating helper logic. Ensure generation guard tests do not spawn FFmpeg/Whisper. Keep tests compatible with `npm test` and existing TypeScript strictness.</action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full Vitest suite passes and covers Phase 3 high-risk logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test doubles->domain behavior | Mocked infrastructure must not hide security behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07-01 | R | test suite | mitigate | Assert observable security outcomes: reject, authorize, redact, cleanup. |
| T-03-07-02 | D | tests | accept | Tests avoid real FFmpeg/Whisper/provider workloads to stay fast and deterministic. |
</threat_model>

<verification>
Run `npm test`, `npm run lint`, and `npm run build`.
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| REQ | OPS-05 automated tests for high-risk flows | This plan |
| REQ | MED-04 upload validation tests | This plan and 03-04 |
| REQ | VID-06 concurrency guard tests | This plan and 03-05 |
| REQ | CAP-06 caption review trigger tests | This plan and 03-06 |
| REQ | SCH-04 schedule validation tests | This plan and 03-06 |
| REQ | SEC-02/SEC-03 ownership/auth mutation tests | This plan plus 03-02/03-03 |
| CONTEXT | Prioritize caption helpers, storage path guards, upload validation, schedule validation, deletion cleanup, concurrency guard | This plan covers the complete list |
</source_audit>

<success_criteria>
`npm test`, `npm run lint`, and `npm run build` pass with focused Phase 3 tests for security and data-integrity risks.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-hardening/03-07-SUMMARY.md`.
</output>
