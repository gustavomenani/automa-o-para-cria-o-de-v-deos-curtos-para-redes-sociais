---
phase: 03-product-hardening
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/features/content/queries.ts
  - src/features/content/actions.ts
  - src/features/content/services/upload-service.ts
  - src/app/dashboard/page.tsx
  - src/app/contents/page.tsx
  - src/app/contents/new/page.tsx
  - src/app/contents/[id]/page.tsx
autonomous: true
requirements: [SEC-02, SEC-03]
must_haves:
  truths:
    - "Authenticated users see only their own projects in dashboard and content lists."
    - "Creating content assigns the project to the current user."
    - "Content detail, generate, regenerate, and delete actions reject projects owned by another user."
    - "Anonymous Server Action calls do not create, mutate, or delete projects."
  artifacts:
    - path: "src/features/content/queries.ts"
      provides: "Owner-scoped content read models"
    - path: "src/features/content/actions.ts"
      provides: "Auth/ownership checks for content mutations"
    - path: "src/features/content/services/upload-service.ts"
      provides: "Project creation with owner id"
  key_links:
    - from: "src/app/dashboard/page.tsx"
      to: "src/features/auth/session.ts"
      via: "requireUser before reads"
      pattern: "requireUser"
    - from: "src/features/content/actions.ts"
      to: "prisma.contentProject"
      via: "where id plus userId"
      pattern: "userId"
---

<objective>
Apply owner scoping to private content UI and Server Actions.

Purpose: SEC-02 and SEC-03 require that pages and form actions authorize each project by owner, not by route visibility.
Output: User-scoped read models and mutations for the dashboard/content surfaces.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-product-hardening/03-CONTEXT.md
@.planning/phases/03-product-hardening/03-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@node_modules/next/dist/docs/01-app/02-guides/authentication.md
@node_modules/next/dist/docs/01-app/02-guides/data-security.md
@node_modules/next/dist/docs/01-app/01-getting-started/07-mutating-data.md
@.planning/phases/03-product-hardening/03-01-SUMMARY.md

<interfaces>
Use `requireUser()` from `src/features/auth/session.ts` created in Plan 03-01.

Current content exports include `getDashboardStats()`, `getContents()`, `getContentById(id)`, `createContentAction(formData)`, `generateContentVideoAction(contentId)`, `generateGeminiAssetsAction(contentId)`, and `deleteContentProjectAction(contentId, redirectTarget)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Scope content read models</name>
  <files>src/features/content/queries.ts, src/app/dashboard/page.tsx, src/app/contents/page.tsx, src/app/contents/[id]/page.tsx</files>
  <behavior>
    - Dashboard counts and recent projects include only `userId === currentUser.id`.
    - Content list includes only the current user's projects.
    - Detail page returns the 03-UI-SPEC forbidden/not-found safe state when the project is not owned by the user.
  </behavior>
  <action>Update query functions to accept `userId` or call the auth DAL internally, then add `where: { userId }` to every content read. In pages, call `requireUser()` before reading private data. Keep existing `dynamic = "force-dynamic"` behavior. Render safe forbidden copy `Voce nao tem acesso a este conteudo.` through existing page/FeedbackBanner patterns; do not leak whether another user's project ID exists.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Private pages build and no content page reads global project data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Assign ownership on create/upload flows</name>
  <files>src/features/content/actions.ts, src/features/content/services/upload-service.ts, src/app/contents/new/page.tsx</files>
  <behavior>
    - Anonymous `createContentAction` redirects to login or safe auth error.
    - Manual and AI-assisted project creation writes `ContentProject.userId`.
    - Upload helper cannot create an ownerless project in normal app entry points.
  </behavior>
  <action>Thread `userId` from `requireUser()` through `createContentAction` into `createContentProject()` and `createProjectWithUploads()`. Update service signatures with typed object params when needed. Keep manual upload fallback and Phase 2 Manus/Gemini behavior intact; do not change provider priority.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>All app create paths assign ownership and preserve existing success redirects.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Authorize content mutations</name>
  <files>src/features/content/actions.ts</files>
  <behavior>
    - Generate/regenerate requires the current user to own the project.
    - AI asset generation requires ownership.
    - Delete captures storage paths only for an owned project and rejects cross-user deletes.
  </behavior>
  <action>Add ownership checks inside every exported content Server Action before side effects. Use `where: { id: contentId, userId: currentUser.id }` or an equivalent owner-scoped read; return the safe forbidden copy from 03-UI-SPEC. Preserve existing revalidation and redirect query parameter patterns.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Server Actions treat direct POST calls as untrusted and authorize by owner before mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser->Server Actions | Direct POST can invoke actions without using the UI. |
| route params->database | Project IDs are attacker-controlled identifiers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | E | `src/features/content/actions.ts` | mitigate | Call `requireUser()` inside each action and query by `{ id, userId }` before side effects. |
| T-03-02-02 | I | `src/features/content/queries.ts` | mitigate | Filter every list/detail read by `userId` and return safe forbidden/not-found UI. |
| T-03-02-03 | T | `src/features/content/services/upload-service.ts` | mitigate | Require a trusted server-supplied owner id for project creation. |
</threat_model>

<verification>
Run `npm run lint` and `npm run build`.
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| GOAL | Secure MVP before external use | This plan plus 03-01 and 03-03 |
| REQ | SEC-02 projects/files/schedules/settings restricted to owner | This plan covers content UI/actions; 03-03 covers API/files/settings; 03-06 covers schedule mutations |
| REQ | SEC-03 mutation routes protected by auth | This plan covers Server Actions |
| CONTEXT | Anonymous users must not create/read/mutate/delete project data | This plan |
| UI-SPEC | Private routes render no private data before auth check | This plan |
| Deferred | Queue/object storage/social publishing/teams | Not planned |
</source_audit>

<success_criteria>
Content pages and Server Actions are owner-scoped, and direct action calls cannot operate on anonymous or cross-user projects.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-hardening/03-02-SUMMARY.md`.
</output>
