---
phase: 03-product-hardening
plan: 06
type: execute
wave: 4
depends_on: [03-02, 03-05]
files_modified:
  - src/app/contents/[id]/page.tsx
  - src/features/content/actions.ts
  - src/features/content/components/caption-review-form.tsx
  - src/features/schedule/actions.ts
  - src/app/schedule/page.tsx
  - src/lib/api-response.ts
autonomous: true
requirements: [CAP-06, REV-04, SCH-04]
must_haves:
  truths:
    - "Low-confidence or fallback captions show `Revise a legenda` before download/schedule decisions."
    - "User can save a revised caption text without a full timeline editor."
    - "New schedules in the past are rejected server-side and in the form UI."
    - "User-facing errors are stable Portuguese copy; raw diagnostics remain server-side."
  artifacts:
    - path: "src/features/content/components/caption-review-form.tsx"
      provides: "Caption edit affordance for low-confidence captions"
    - path: "src/features/schedule/actions.ts"
      provides: "Past-date validation before ScheduledPost persistence"
    - path: "src/lib/api-response.ts"
      provides: "Redacted API error responses"
  key_links:
    - from: "src/app/contents/[id]/page.tsx"
      to: "src/features/content/components/caption-review-form.tsx"
      via: "render warning and edit form when caption confidence is low"
      pattern: "Revise a legenda"
    - from: "src/features/schedule/actions.ts"
      to: "prisma.scheduledPost.create"
      via: "scheduledAt > now guard"
      pattern: "scheduledAt"
---

<objective>
Harden review UX, schedule validation, and redacted error handling.

Purpose: CAP-06, SCH-04, and REV-04 are user-facing hardening requirements that reduce bad outputs and prevent unsafe/internal error disclosure.
Output: Caption review form, schedule past-date guard, and shared safe error normalization.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-product-hardening/03-CONTEXT.md
@.planning/phases/03-product-hardening/03-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@node_modules/next/dist/docs/01-app/01-getting-started/10-error-handling.md
@node_modules/next/dist/docs/01-app/02-guides/forms.md
@.planning/phases/03-product-hardening/03-02-SUMMARY.md
@.planning/phases/03-product-hardening/03-05-SUMMARY.md

<interfaces>
Caption warning source exists from Phase 1 as `CAPTION_SYNC_WARNING` and `ContentProject.errorMessage` usage. Existing `FeedbackBanner` supports `success`, `info`, and `error`.

Existing schedule action exports `schedulePostAction(formData)` and already parses date/time before creating `ScheduledPost`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add low-confidence caption review affordance</name>
  <files>src/features/content/components/caption-review-form.tsx, src/app/contents/[id]/page.tsx, src/features/content/actions.ts</files>
  <behavior>
    - When caption confidence is low or fallback sync was used, page shows `Revise a legenda` above the editable caption area.
    - Saving revised caption updates `ContentProject.caption` for the owned project.
    - No waveform, timeline, scene-level, or full subtitle editor is introduced.
  </behavior>
  <action>Create a feature component for editing only the existing caption text. Add `saveCaptionReviewAction(contentId, formData)` or equivalent owner-scoped Server Action. Render the warning banner and form on the detail page when persisted warning/quality state indicates uncertain sync. Keep copy from 03-UI-SPEC and existing review layout.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Low-confidence captions have a practical review/edit path before users regenerate, download, or schedule.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate schedules are in the future</name>
  <files>src/features/schedule/actions.ts, src/app/contents/[id]/page.tsx, src/app/schedule/page.tsx</files>
  <behavior>
    - Server rejects `scheduledAt <= new Date()` before creating `ScheduledPost`.
    - Detail page date/time controls guide users toward future values.
    - Past-date failure shows `Escolha uma data e horario futuros para salvar o agendamento.`
  </behavior>
  <action>Add server-side past-date validation in `schedulePostAction` before persistence. Preserve owner checks from Phase 3. Set useful `min` guidance in the schedule form, but do not rely on browser validation alone. Keep schedule list display behavior for existing due posts.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>New past schedules cannot be saved by crafted form submissions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Centralize redacted errors for API/action feedback</name>
  <files>src/lib/api-response.ts, src/features/content/actions.ts, src/features/schedule/actions.ts</files>
  <behavior>
    - Raw paths, stack traces, SQL, FFmpeg commands, provider payloads, request headers, and env var names are not returned to users.
    - Detailed diagnostics are logged server-side with a correlation id or clear prefix.
    - Expected validation errors use stable Portuguese copy.
  </behavior>
  <action>Add or reuse a safe error normalizer that maps known validation/domain errors to allowed Portuguese messages and replaces unknown/internal failures with `Nao foi possivel concluir a acao. Revise os dados e tente novamente.` or `Nao foi possivel concluir o processamento. Revise os arquivos enviados e tente novamente.` Update `apiError` and relevant actions to use it. Keep detailed diagnostics in server logs only; do not render raw `error.message` by default.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Review/schedule/API errors are user-safe while preserving server diagnostics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user form->caption update | User-edited caption text enters persistence and rendering. |
| user form->schedule create | User-supplied date/time controls scheduled rows. |
| internal exception->user response | Raw diagnostics could cross into UI/API responses. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | T | caption review action | mitigate | Validate caption length/text and owner-scope update. |
| T-03-06-02 | T | schedule action | mitigate | Compute `scheduledAt` server-side and require future time before create. |
| T-03-06-03 | I | error responses | mitigate | Central safe error normalizer and server-side diagnostic logging. |
</threat_model>

<verification>
Run `npm run lint` and `npm run build`.
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| REQ | CAP-06 manual review before final generation for low confidence | This plan |
| REQ | REV-04 clear errors without sensitive internals | This plan |
| REQ | SCH-04 reject new schedules in the past | This plan |
| CONTEXT | No full timeline editor | This plan explicitly avoids it |
| UI-SPEC | Caption warning, schedule error, redacted copy | This plan |
| Deferred | Worker scheduler/social publishing | Excluded |
</source_audit>

<success_criteria>
Caption uncertainty is reviewable, past schedules are blocked, and user-facing errors are redacted consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-hardening/03-06-SUMMARY.md`.
</output>
