---
phase: 03-product-hardening
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/app/api/content-projects/route.ts
  - src/app/api/content-projects/[id]/media/route.ts
  - src/app/api/content-projects/[id]/generate/route.ts
  - src/app/api/files/[...path]/route.ts
  - src/features/settings/actions.ts
  - src/features/settings/queries.ts
  - src/app/settings/page.tsx
autonomous: true
requirements: [SEC-02, SEC-03, SEC-04]
must_haves:
  truths:
    - "JSON upload/generation APIs reject anonymous requests."
    - "File downloads require the requester to own the project that owns the file path."
    - "Manus settings are read and saved per authenticated user."
    - "Saved provider keys are never returned to the browser."
  artifacts:
    - path: "src/app/api/files/[...path]/route.ts"
      provides: "Owner-authorized file serving"
    - path: "src/features/settings/actions.ts"
      provides: "Owner-scoped settings save"
    - path: "src/features/settings/queries.ts"
      provides: "Owner-scoped settings read DTO"
  key_links:
    - from: "src/app/api/files/[...path]/route.ts"
      to: "MediaFile/GeneratedVideo project.userId"
      via: "path lookup before file read"
      pattern: "userId"
    - from: "src/features/settings/actions.ts"
      to: "ManusSettings.userId"
      via: "upsert by current user"
      pattern: "userId"
---

<objective>
Protect API, file-serving, and settings surfaces with auth and ownership.

Purpose: Pages are not the only entry point. Route Handlers and settings reads/writes must enforce the same owner boundary and secret redaction.
Output: Authenticated APIs, owner-authorized file route, and per-user settings behavior.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-product-hardening/03-CONTEXT.md
@.planning/phases/03-product-hardening/03-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@node_modules/next/dist/docs/01-app/01-getting-started/15-route-handlers.md
@node_modules/next/dist/docs/01-app/02-guides/authentication.md
@node_modules/next/dist/docs/01-app/02-guides/data-security.md
@.planning/phases/03-product-hardening/03-01-SUMMARY.md

<interfaces>
Existing API routes use `apiCreated()` and `apiError()` from `src/lib/api-response.ts`. Existing Node-only upload/generate routes export `runtime = "nodejs"`. Keep Node runtime for filesystem/Prisma/process routes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authenticate and authorize content APIs</name>
  <files>src/app/api/content-projects/route.ts, src/app/api/content-projects/[id]/media/route.ts, src/app/api/content-projects/[id]/generate/route.ts</files>
  <behavior>
    - Anonymous POST requests return 401 with safe JSON.
    - Cross-user project media/generate requests return 403 or safe not-found JSON.
    - New API-created projects are assigned to the authenticated user.
  </behavior>
  <action>Call the session helper in each Route Handler. For project-specific routes, verify project ownership before parsing expensive bodies or invoking services. Keep `export const runtime = "nodejs"`. Return stable Portuguese error JSON; do not return raw exception messages from Prisma, FFmpeg, provider APIs, or local paths.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>All content JSON APIs require auth and project ownership before mutation or generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Protect local file serving by owner</name>
  <files>src/app/api/files/[...path]/route.ts</files>
  <behavior>
    - Anonymous file requests return 401.
    - Authenticated users can download only paths attached to their MediaFile or GeneratedVideo records.
    - Traversal protection remains in place before disk access.
  </behavior>
  <action>Before reading from disk, resolve the requested absolute path under `storageRoot` and look it up in `MediaFile.path` or `GeneratedVideo.path` with `project.userId === currentUser.id`. Preserve existing traversal checks and content-type behavior. If no owned row maps to the path, return safe 404/403 JSON and do not reveal whether the file exists on disk.</action>
  <verify>
    <automated>npm run lint</automated>
  </verify>
  <done>Knowledge of `/api/files/...` paths is no longer enough to download another user's media.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Scope settings by user and redact secrets</name>
  <files>src/features/settings/actions.ts, src/features/settings/queries.ts, src/app/settings/page.tsx</files>
  <behavior>
    - Settings page requires login.
    - `getManusSettings()` returns only the current user's settings DTO and never the raw API key.
    - Saving settings upserts the current user's row.
  </behavior>
  <action>Use `requireUser()` in settings query/action/page. Replace newest-global-row behavior with owner-scoped lookup/upsert. Keep raw `apiKey` server-side only; return masked metadata/booleans to the page. Preserve Phase 2 provider copy and do not implement teams or social credential management.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Settings are per-user, authenticated, and secret-bearing fields are not rendered back to the browser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| external caller->Route Handler | APIs can be called without the app UI. |
| URL path->local filesystem | File paths are attacker-controlled URL segments. |
| settings form->database | Secret-bearing provider key crosses into persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | S | API routes | mitigate | Verify signed session before route work. |
| T-03-03-02 | E | API routes | mitigate | Check `ContentProject.userId` before media/generate actions. |
| T-03-03-03 | I | file route | mitigate | Authorize DB ownership before disk read and use indistinct safe errors. |
| T-03-03-04 | I | settings page/query | mitigate | Return DTO without raw `apiKey`; render masked metadata only. |
</threat_model>

<verification>
Run `npm run lint` and `npm run build`.
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| REQ | SEC-02 owner restriction for files and settings | This plan |
| REQ | SEC-03 auth on mutation routes | This plan |
| REQ | SEC-04 no secret rendering | This plan and 03-06 |
| CONTEXT | Auth connects to API routes and file serving | This plan |
| UI-SPEC | Settings/private surfaces block unauthenticated data | This plan |
| Deferred | Object storage migration | Excluded; local file route is hardened only |
</source_audit>

<success_criteria>
API routes, file downloads, and settings cannot be used anonymously or across user ownership boundaries.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-hardening/03-03-SUMMARY.md`.
</output>
