---
phase: 03-product-hardening
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - src/features/video/services/video-service.ts
  - src/features/video/services/video-service.test.ts
  - src/lib/storage/local-storage.ts
  - src/app/api/content-projects/[id]/generate/route.ts
  - src/features/content/actions.ts
  - src/features/content/components/generate-video-button.tsx
autonomous: true
requirements: [VID-06, SEC-03]
must_haves:
  truths:
    - "Only one generation can run for the same project at a time."
    - "A second generate request returns a safe locked-state message without starting FFmpeg/Whisper."
    - "Generated output paths do not corrupt an in-progress or previous generation."
    - "Generate UI disables the action while the project is processing."
  artifacts:
    - path: "src/features/video/services/video-service.ts"
      provides: "Transactional generation lock and safe lock errors"
    - path: "src/features/video/services/video-service.test.ts"
      provides: "Concurrency guard tests"
    - path: "src/features/content/components/generate-video-button.tsx"
      provides: "Locked/pending UI state"
  key_links:
    - from: "src/features/content/actions.ts"
      to: "src/features/video/services/video-service.ts"
      via: "generation lock result"
      pattern: "generateProjectVideo"
    - from: "src/app/api/content-projects/[id]/generate/route.ts"
      to: "src/features/video/services/video-service.ts"
      via: "same guard as server action"
      pattern: "generateProjectVideo"
---

<objective>
Prevent concurrent video generation corruption.

Purpose: VID-06 requires duplicate generate/regenerate requests to be deduplicated or rejected before they can race on project status and MP4 output.
Output: Server-side lock, safe user feedback, unique output handling, and UI disabled state.
</objective>

<execution_context>
@C:/Users/User/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/User/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-product-hardening/03-CONTEXT.md
@.planning/phases/03-product-hardening/03-UI-SPEC.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/TESTING.md
@.planning/phases/03-product-hardening/03-02-SUMMARY.md
@.planning/phases/03-product-hardening/03-03-SUMMARY.md

<interfaces>
Current `videoService.generateProjectVideo(projectId, options?)` is called by `generateContentVideoAction()` and `POST /api/content-projects/[id]/generate`.

Current `getGeneratedVideoPath(contentId)` writes `storage/generated/<contentId>.mp4`, which is vulnerable to overlapping writes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add server-side generation lock tests</name>
  <files>src/features/video/services/video-service.test.ts</files>
  <behavior>
    - When project status is already PROCESSING, generation rejects with `Ja existe uma geracao em andamento para este projeto. Aguarde a conclusao antes de tentar novamente.`
    - When two calls race, only one transitions the project from non-processing to PROCESSING.
    - Rejected locked calls do not create a GeneratedVideo row or output path.
  </behavior>
  <action>Create Vitest tests around a lock helper or mocked Prisma transaction. Mock FFmpeg/ffprobe/transcription so tests assert state transitions only. The RED state should fail before implementation.</action>
  <verify>
    <automated>npm test -- --run src/features/video/services/video-service.test.ts</automated>
  </verify>
  <done>Concurrency tests fail before implementation and pass after Task 2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement transactional generation lock and unique output paths</name>
  <files>src/features/video/services/video-service.ts, src/lib/storage/local-storage.ts</files>
  <behavior>
    - Generation acquires a project-level lock with a conditional update or transaction.
    - Lock failure returns the UI-spec locked message.
    - Output path includes the generated video id or another per-run unique suffix.
  </behavior>
  <action>Before expensive processing, atomically transition the owned project from a non-PROCESSING status to PROCESSING. If zero rows update, return/throw the locked-state copy from 03-UI-SPEC. Create the `GeneratedVideo` row before computing the final path, then use a unique generated path such as `storage/generated/<projectId>/<generatedVideoId>.mp4` or equivalent. Preserve existing READY/ERROR status updates and safe Portuguese processing errors.</action>
  <verify>
    <automated>npm test -- --run src/features/video/services/video-service.test.ts</automated>
  </verify>
  <done>Only one generation proceeds per project and outputs are unique per run.</done>
</task>

<task type="auto">
  <name>Task 3: Wire locked state through actions, API, and button UI</name>
  <files>src/app/api/content-projects/[id]/generate/route.ts, src/features/content/actions.ts, src/features/content/components/generate-video-button.tsx</files>
  <action>Map the lock error to safe JSON for the API and safe redirect/banner feedback for the Server Action. Disable generate/regenerate buttons when the project status is PROCESSING and show `Gerando video...` or the locked-state copy. Use `disabled:cursor-not-allowed`; do not rely only on opacity.</action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Double-clicks and duplicate API requests produce safe locked feedback instead of concurrent processing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/API->generation service | Untrusted callers can retry expensive generation. |
| generation service->filesystem | Generated MP4 output is written to local disk. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | D | `video-service.ts` | mitigate | Atomic lock prevents duplicate CPU/IO work per project. |
| T-03-05-02 | T | `local-storage.ts` | mitigate | Unique output paths prevent overlapping writes from corrupting files. |
| T-03-05-03 | R | actions/API | mitigate | Return stable locked feedback; detailed diagnostics remain server-side. |
</threat_model>

<verification>
Run `npm test -- --run src/features/video/services/video-service.test.ts`, `npm run lint`, and `npm run build`.
</verification>

<source_audit>
## Multi-Source Coverage Audit

| Source | Item | Covered By |
|--------|------|------------|
| REQ | VID-06 avoid corruption in concurrent generation | This plan |
| REQ | SEC-03 protect expensive mutation routes against abuse | This plan plus 03-03 |
| CONTEXT | Prevent concurrent generation for same project | This plan |
| UI-SPEC | Locked generation label/banner and disabled button | This plan |
| Deferred | Queue/worker execution | Excluded; this plan adds an in-process lock only |
</source_audit>

<success_criteria>
Concurrent generation attempts for the same project cannot start duplicate media processing or overwrite the same output file.
</success_criteria>

<output>
After completion, create `.planning/phases/03-product-hardening/03-05-SUMMARY.md`.
</output>
